import Screen from "../components/Screen";
import { Text } from "react-native";

const AccountScreen = () => {
  return (
    <Screen>
      <Text>Accounts</Text>
    </Screen>
  );
};

export default AccountScreen;
